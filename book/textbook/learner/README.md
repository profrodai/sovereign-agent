# Your constructed definitions

Chapter 2 explains how to save tool schemas, handlers and dispatch in `ch02.py`. Chapter 3 adds the owned model/tool loop and adapter in `ch03.py`. The included files are completed comparison implementations; work in your own checkout and retain your attempts before replacing them with a comparison.

The Chapter 3 checkpoint loads these files, so changing their essential behavior changes the executable result. The live adapter still uses supplied bounded HTTP transport. Later reference checkpoints import other supplied runtime components. See [code ownership](../OWNERSHIP.md) and the [construction roadmap](../EXPANSION.md) before treating these two files as a finished nineteen-chapter agent.

# Packaging fragment

The wheel includes every runtime module and contract schema. The release gate
installs the core wheel without extras in a clean environment, validates schema
resources and CLI startup, and rejects import-time filesystem, network, or
process side effects.

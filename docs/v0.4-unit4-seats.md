# v0.4 Unit 4 — seat supervisor

Presence is runtime observation: `starting`, `online`, `busy`,
`awaiting-approval`, `draining`, `offline`, `failed`, `unknown`. Duplicate
coordinators cannot lease one exclusive instance. Provider-session
mismatch emits a continuity-break event.

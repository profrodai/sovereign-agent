# v0.4 Unit 5 — approvals

Durable approval records survive process restart. Absence of a matching
policy defaults to `require-human` for external writes and destructive
actions. Autonomous engage mode does not authorize email send. An
approved decision resumes exactly one execution.

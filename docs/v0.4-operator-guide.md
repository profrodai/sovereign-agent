# v0.4 operator guide

```bash
sovereign-agent connect zeo probe
sovereign-agent request submit request.json --config runtime.json
sovereign-agent request status exec_...
sovereign-agent receipt export exec_... --config runtime.json --format json
sovereign-agent receipt verify receipt.json
sovereign-agent api serve --runtime-root PATH --socket /tmp/sa.sock --secret ...
sovereign-agent service status --runtime-root PATH
sovereign-agent service backup DEST --runtime-root PATH
sovereign-agent service restore ARCHIVE --destination NEW --verify-only
sovereign-agent status --runtime-root PATH --json
sovereign-agent seats list --runtime-root PATH --json
sovereign-agent executions list --state running --runtime-root PATH --json
sovereign-agent relay inspect --conversation round_351 --runtime-root PATH
sovereign-agent approvals-v04 pending --runtime-root PATH --json
sovereign-agent capabilities --verified-only --runtime-root PATH --json
```

The teaching `sovereign-agent serve` command remains the v0.2/v0.3
orchestrator. The v0.4 coordinator is `service` plus `api serve`.

# v0.5 Unit 7 — compatibility window

`register_tool`, `ToolRegistry`, `ToolResult`, and `make_builtin_registry`
remain public. `register_tool` emits `DeprecationWarning`. Legacy tools can
be adapted through `registered_tool_to_bound`.

# v0.4 Unit 6 — channels and delivery receipts

CLI, authenticated webhook, Slack (optional extra), and email-draft
adapters normalize inbound events and record outbound delivery receipts
independently of execution receipts. Duplicate inbound webhooks apply
once. Email send cannot bypass approval policy.

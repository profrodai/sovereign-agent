# v0.5 Unit 5 — policy and concurrency

Approval is evaluated before invocation. `parallelism_policy="always"` is
not a product option. ZeoCore `ConcurrencyMode` declarations drive
Sovereign lock acquisition.

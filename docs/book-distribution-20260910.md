# Discoverable teaching files and Prof Rod attribution

Created 2026-09-10. This supplements the [four-asset migration](book-migration-20260909.md); it does not rewrite the earlier edition record.

The active book keeps textbook, exercises, solutions and educator directories. All teaching files use the `profrod-sovereign-agent` namespace, a zero-padded chapter where relevant, a topic and an audience/role. A practice letter preserves teaching order. For example, `profrod-sovereign-agent-ch02-a-pydantic-shop-tools-exercise.ipynb` and `profrod-sovereign-agent-ch02-b-pydantic-validation-repair-solution.ipynb` can be identified outside their original folders. Educator copies carry `educator-exercise` or `educator-solution` and retain identical teaching content. Matching Markdown uses the same stem. Python helpers use underscores for equivalent descriptive names.

Each asset has a named `start-here.md` document. Only the five fixed GitHub README gateways and `textbook/BOOK.json` keep conventional names. Gateways are not included in the companion ZIPs; all files in the three downloads have unique basenames even when combined. ZIP names carry the namespace, audience and 2026-09-10 distribution version. The named start page has its own self-download link replaced with an already-downloaded statement inside the ZIP; all other bytes match their source files.

Every active notebook opens with Prof Rod, the book title, https://profrod.ai/book, an explicit invitation to https://profrod.ai/community, and the original GitHub source. Its final cell invites readers to bring a result or question to the community and share the resource with its provenance. Matching Markdown retains these blocks. Chapter documents, guides, setup pages and code assets carry equivalent source and community links. Notebook metadata also records the canonical resource ID and distribution version. No runtime analytics or network requests were added.

The 64 primary notebooks retain their previous code-cell sequences; their matching educator copies remain identical. The nineteen-chapter registry retains stable lesson identities and explicit PLANNED status for Chapters 4, 7 and 13. Old published links still refer to their immutable original revision. The [rename inventory](evidence/book-four-assets/distribution-migration-v1.json) maps every previous active path to its new location and records line-count and notebook-code preservation checks.

Run `python scripts/verify_book_assets_v2.py --textbook` for the current manuscript and checkpoints. Run `python scripts/verify_book_assets_v2.py --check-execution` in the authoring environment for every current notebook and handoff. Run `python scripts/package_book_assets_v2.py --verify` for archive membership, source hashes, unique shared names and local navigation. The full gate remains `make verify`; `make exercises` runs the complete notebook execution gate. The earlier v1 instruments and receipts remain historical records and are not the active edition gate.

Website consumers continue to use `book/textbook/BOOK.json` and each row's `path` and `checkpoint`. Resolve those fields as data; do not reconstruct the old `chNN/README.md` or `checkpoints/chNN.py` conventions. This change adds source links to distributed material; it does not deploy or re-sync the website.

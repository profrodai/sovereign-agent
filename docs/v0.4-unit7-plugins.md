# v0.4 Unit 7 — plugin entry points

Entry points are discovered without instantiation. Allowlist and
`api_range` checks run before `load()`. Optional plugin failures do not
prevent the base service from starting. Plugins cannot register
governance authority.

# v0.5 Unit 3 — registry and projection

Reusable actions use ZeoCore `CapabilityRegistry`. Sovereign freezes a
per-execution catalog and reverse-maps OpenAI function names. Duplicate or
truncated projection names are refused.

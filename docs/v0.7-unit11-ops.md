# v0.7 Unit 11 — fleet operations

`FleetMetrics` exposes health, lease age, queue, reservations, capacity,
refusal, policy, transfer, and reconciliation counters at low cardinality.
Identifiers belong in evidence. CLI: `sovereign-agent fleet worker
list|show|drain|probe`, `fleet execution locate|reconcile`, `fleet status`.
Rolling upgrades support protocol N / N-1.

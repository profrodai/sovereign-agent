# v0.7 Unit 2 — capability-aware placement

`PlacementEngine` matches frozen ZeoCore requirements/effects and runtime
constraints against verified worker manifests. Isolation and network must be
`EvidenceLevel.ENFORCED`. Declared or probed claims are refusals. Selection and
every candidate rejection reason are persisted. Isolation and network policy
never silently degrade.

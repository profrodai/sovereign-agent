# v0.4 Unit 2 — Zero Employee connector

`connectors.zero_employee` accepts request bytes or an immutable path,
validates `GovernedExecutionRequest`, and returns transport
acknowledgement independently of receipt finalization. The connector
never imports `zero_employee` Python internals.

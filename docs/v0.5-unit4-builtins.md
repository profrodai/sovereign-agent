# v0.5 Unit 4 — built-ins

Session filesystem operations are ZeoCore-authored capabilities with
OpenAI projection names `read_file`, `write_file`, and `list_files`.
`complete_task` and `handoff_to_structured` are runtime commands.
`CallableSurface` merges both for the provider tools parameter.

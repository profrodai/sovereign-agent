# v0.5 Unit 8 — removal gate

The legacy tool surface is still exported. Removal requires HITL, session,
projection, concurrency, and cancellation tests plus a separately approved
breaking-release change. This unit records the gate; it does not delete
`register_tool`.

# v0.4 Unit 8 — service operations

One coordinator lease fences a runtime root using UUID, PID, host,
start marker, heartbeat, and fencing token. Readiness and liveness are
distinct. Backup never overwrites a live root on restore. Retention
writes a compaction manifest.

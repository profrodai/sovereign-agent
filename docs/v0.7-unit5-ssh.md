# v0.7 Unit 5 — SSH worker

`SshWorker` pins host identity (`StrictHostKeyChecking=yes` and a dedicated
known-hosts file). Unattended TOFU is refused. Disconnect leaves the
execution in explicit `unknown` until Unit 10 reconciles. Inputs are
content-addressed; writes stay inside the execution root. Workers cannot
finalize after fencing expiry.

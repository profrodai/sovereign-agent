# v0.5 Unit 6 — evidence

Capability calls persist a ZeoCore `CapabilityInvocationRecord` and emit
`executor.capability_invoked` with digests, not raw arguments. Receipts may
carry `capability_catalog_digest` and `capability_invocation_refs`.

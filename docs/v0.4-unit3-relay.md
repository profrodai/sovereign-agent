# v0.4 Unit 3 — durable relay v2

Relay records live in `inbox/`, `claimed/`, `acknowledged/`, `outbox/`,
and `dead-letter/` under hashed instance directories. Claims carry owner,
PID, lease deadline, and attempt number. Expired claims return to inbox
with a recovery event. v1 `messages/` records are migrated on open.

# v0.5 Unit 2 — runtime adapter

`CapabilityExecutor` and `CapabilityContextFactory` live in
`sovereign_agent.capabilities`. ZeoCore 0.5.0 `invoke_async` is not used for
cancellable work; `invoke_cancellable` re-raises `CancelledError`.

# v0.4 Unit 9 — release compatibility

A v0.3 runtime root upgrades through copy-on-write migration into a new
tree (`layout_version` 2). The original tree is left untouched so
rollback is a restore of that original. Optional extras: `slack`, `email`.

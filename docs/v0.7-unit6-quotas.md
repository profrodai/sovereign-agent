# v0.7 Unit 6 — reservations and quotas

`ReservationLedger` reserves atomically and releases idempotently. CPU, memory,
disk, PIDs, wall-time, concurrency, and token limits are enforced. Governance
budgets stay distinct from runtime measurement. Requested, reserved, peak, and
consumed vectors are recorded for the receipt.

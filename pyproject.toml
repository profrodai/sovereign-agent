[project]
name = "sovereign-agent"
version = "1.0.0.dev1"
description = "An executable textbook for learning Zero-Employee Organizations"
readme = "README.md"
requires-python = ">=3.14"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [{ name = "Rod Rivera" }]
keywords = ["agents", "governance", "education", "zero-employee-organization"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Education",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.14",
  "Topic :: Education",
]
dependencies = ["pydantic>=2,<3"]

[project.scripts]
sovereign-agent = "sovereign_agent.cli:main"

[project.urls]
Homepage = "https://github.com/zeroemployeeorg/sovereign-agent"
Repository = "https://github.com/zeroemployeeorg/sovereign-agent"
Issues = "https://github.com/zeroemployeeorg/sovereign-agent/issues"

[dependency-groups]
dev = [
  "build>=1.3",
  "mypy>=1.18",
  "pytest>=8",
  "ruff>=0.13",
]

[build-system]
requires = ["setuptools>=80", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
include = ["sovereign_agent*", "reference_organizations*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra -m 'not live'"
markers = [
  "live: installed provider probes/assignments; excluded from default CI",
]

[tool.ruff]
line-length = 100
target-version = "py314"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B"]

[tool.mypy]
python_version = "3.14"
strict = true

# Capability receipt example

Offline proof that projection, invocation, evidence, receipt, restart,
and digest verification share one capability-native path.

```bash
python -m examples.capability_receipt.run
```
